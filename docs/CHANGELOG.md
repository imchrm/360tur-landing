# CHANGELOG — 360tur-landing

Формат: `[дата] — описание изменений`.

---

## 2026-04-19 — Развёртка React + Vite проекта в корне, импорт дизайна из Stitch

**Build setup:**
- В корень добавлены `package.json` (с зависимостями React 18, React Router 6, Vite 5, Tailwind 3), `vite.config.js`, `tailwind.config.js` (палитра и шрифты из `DESIGN_BRIEF.md`), `postcss.config.js`, `index.html` (entry для Vite).
- `src/main.jsx`, `src/App.jsx` (`BrowserRouter`, единственный маршрут `/`), `src/index.css` (директивы Tailwind + Material Symbols).
- `npm install`, `npm run build` проходят успешно (≈ 57 КБ gzip JS).

**i18n:**
- Реализован `src/contexts/LanguageContext.jsx`: автодетект языка по `navigator.language`, персистентность в `localStorage`, хук `useLanguage()` с функцией `t(key)` и поддержкой вложенных ключей.
- Добавлены `src/locales/ru.js` (полный словарь из дизайна), `uz.js` и `en.js` (временно ре-экспортируют `ru.js` до перевода).

**Компоненты (`src/components/`):**
- HTML из `design/site_01/code.html` разнесён по компонентам: `Header`, `Navigation`, `LanguageSwitcher` (рабочий, с `LanguageContext`), `Hero`, `AboutCards`, `Services`, `Portfolio`, `FAQ`, `Footer`. Добавлен утилитарный `Icon.jsx` (обёртка над Material Symbols).
- Все строки вынесены в локали; цвета и шрифты — через токены Tailwind.
- Известные баги дизайна перенесены в `TODO.md` (Приоритет 4): нечитаемый Hero-заголовок, нерабочий dropdown «Контакты», заглушки соцсетей и iframe портфолио.

**TODO.md:**
- Закрыты пункты Приоритета 1 (создание `src/`), Приоритета 2 (LanguageSwitcher / Telegram / маршруты / `App.jsx`), частично Приоритета 3 (палитра, шрифты).
- Добавлены новые приоритеты: «Доводка дизайна» (баги Stitch-импорта), «Недостающие секции» (HowWeWork / Testimonials / About не были сгенерированы Stitch), «Маршруты и юридические страницы», «SEO и аналитика».

**ARCHITECTURE.md:** пути конфигов обновлены — теперь они в корне (раньше отмечалось «сейчас в `musor/`»).

**CONTEXT.md:** актуализированы разделы «Структура репозитория», «Текущее состояние», «Следующий шаг».

## 2026-04-19 — DESIGN_BRIEF.md: бриф для дизайн-агента

- Создан `docs/DESIGN_BRIEF.md` — структурированный бриф по итогам опроса из 4 фаз: бизнес и аудитория, бренд и визуальный стиль, структура секций и контент, интерактив/анимации/SEO.
- Зафиксированы: палитра (`#5C092C`, `#6C1C0B`, `#3B0847` на белом), гротескная типографика, минимум движения, `Marzipano` в Hero, цель Lighthouse ≥ 80, WCAG 2.1 AA, hreflang + `LocalBusiness`, аналитика (Y.Metrica + GA4), формат поставки — готовый React-проект.
- Добавлены открытые вопросы для следующей итерации с клиентом (альтернативный подзаголовок Hero, мобильный CTA, sticky-хедер, перечень соцсетей/услуг и т.д.).
- `README.md`: добавлена ссылка на `DESIGN_BRIEF.md`.

## 2026-04-19 — Правило синхронизации CONTEXT.md ↔ TODO.md

- В шапку `CONTEXT.md` добавлено правило: при изменении чекбоксов в `TODO.md` в том же коммите обновлять разделы «Текущее состояние» и «Следующий шаг».
- В `TODO.md` (раздел «Регулярные задачи») добавлен соответствующий пункт как зеркальное напоминание.

## 2026-04-19 — README.md: описание проекта

- Наполнен `README.md`: описание, тех-стек, краткая архитектура, структура репозитория, ссылки на `docs/`, инструкции запуска из шаблона `musor/`, конвенция веток.
- В `TODO.md` отмечен выполненным пункт Приоритета 1 «Составить описание проекта и внести в `README.md`».

## 2026-04-19 — Документация: уточнение и расширение

**CONTEXT.md**
- Раздел «Машина разработки» дополнен описанием web-сессии Claude (Linux, `/home/user/360tur-landing`).
- Одноразовая корректировка раздела «Структура репозитория»: добавлен `package.json` в корне, исправлена вложенность файлов в `musor/`.
- Добавлена категория веток `claude/*` (для задач, выполняемых агентом Claude).
- Удалён раздел «Открытые вопросы» (вопросы централизованы в `TODO.md`).

**TODO.md**
- Задача `npm init -y` отмечена выполненной.
- Вопрос про `LanguageSwitcher` переформулирован в задачу: «Модифицировать для поддержки `ru / uz / en`».
- Задача в Приоритете 4 уточнена: «Переработать визуал (layout) `LanguageSwitcher.jsx`».
- Удалён дублирующийся пункт о ручной корректировке структуры (заменён регулярной задачей).
- Добавлен раздел «Регулярные задачи» — поддерживать актуальность раздела «Структура репозитория» в `CONTEXT.md`.
- Незакрытый вопрос про удаление графики в `musor/public/images` перенесён в «Отложено / под вопросом».

**ARCHITECTURE.md**
- Добавлены разделы: «Тип приложения» (SPA, статический хостинг), «Структура `src/`», «Ассеты», «Интернационализация (i18n)», «Управление состоянием», «Формы».
- Зафиксировано: i18n — собственная реализация на React Context + плоских словарях `src/locales/{ru,uz,en}.js`, без `i18next`.
- Зафиксировано: state — `useState` + React Context API, без Redux/Zustand.
- Зафиксировано: роутер — `BrowserRouter` + оговорка про SPA-fallback.
- Пути конфигов сборщика и Tailwind указаны как целевые (корень репозитория) с пометкой о текущем расположении в `musor/`.
- Плейсхолдеры `[дата]` в таблице решений заменены реальными датами.

**CHANGELOG.md**
- Запись «Инициализация» датирована (2026-04-17).
- Из записи «Инициализация» убраны упоминания `LEAD_FORM_SETUP.md` и переименования ветки `master → main`.
- Упрощён формат в шапке (убрано упоминание «ветка/коммит»).

## 2026-04-17 — Инициализация

- Scaffold из шаблона musorku.
- Удалена папка `telegram/` (до первого коммита).
- Настроен `.gitignore`.
- Создана структура `docs/`.

# TODO — 360tur-landing

## Статус проекта
React + Vite + Tailwind проект развёрнут в корне репозитория, секции из дизайна Stitch разнесены по компонентам. Идёт устранение багов и наполнение контентом.

---

## Приоритет 1 — Приготовление

- [x] Составить описание проекта и внести в README.md
- [x] Создать папку с исходниками проекта (`src/` в корне; шаблон `musor/` остаётся как референс).
- [x] Инициализировать проект (`npm init -y`) — `package.json` создан в корне репозитория

## Приоритет 2 — Очистка шаблона

- [x] Модифицировать `LanguageSwitcher` для поддержки языков `ru / uz / en` (новый `src/components/LanguageSwitcher.jsx` + `LanguageContext`)
- [x] Удалить логику Telegram-бота — в новом `Footer.jsx` / `Hero.jsx` бот не используется (старый `musor/src/components/Contact.jsx` ещё лежит как референс, но не подключается)
- [x] Убрать маршруты и страницы, не нужные для нового лендинга — `App.jsx` содержит только `/`
- [x] Проверить `App.jsx` на наличие мёртвого кода после удалений

## Приоритет 3 — Брендинг

- [x] Обновить цветовую схему в `tailwind.config.js` (палитра из `DESIGN_BRIEF.md`)
- [x] Обновить шрифты (Manrope для headline, Inter для body/label)

## Приоритет 4 — Доводка дизайна (баги после Stitch-импорта)

- [x] Hero: сделать заголовок читаемым на фоне изображения — добавлены градиент-overlay (`from-surface via-surface/5 to-transparent`) и утилита `.text-glow-surface` в `src/index.css`
- [x] Header «Контакты»: dropdown упразднён, заменён на кликабельный номер телефона (видимый номер на десктопе, иконка `call` на мобильных). Старый `ContactsDropdown.jsx` удалён.
- [x] Hero: 3 icon-кнопки мессенджеров вместо двух — Telegram (`send`), WhatsApp (`chat`), Instagram (`photo_camera`); открываются в новой вкладке. URL-плейсхолдеры (`https://t.me/username`, `https://wa.me/998900000000`, `https://instagram.com`).
- [x] Portfolio: подключён iframe демо-тура Marzipano (`/tours/neoclassicalbedroom/index.html`) из `public/tours/`
- [ ] Portfolio: создать карусель из нескольких демо-туров Marzipano
- [ ] Обновить `<title>` и meta-теги в `index.html` (description, OG, hreflang, Schema.org `LocalBusiness`)
- [ ] Footer: подставить реальные ссылки на соцсети (Instagram / Facebook / Telegram-канал) — нужны URL от клиента (плейсхолдер `language` иконка пока на месте)
- [ ] Заменить логотип и favicon (`public/`)
- [ ] Подставить реальные номера телефонов в `tel:`-ссылках (Header, Hero, Footer) — все три используют общий placeholder `+998 90 000 00 00`

## Приоритет 5 — Контент

- [x] Заменить картинку Hero — файл `public/imgs/bedroom_cam_001_res_4096x2048_00.jpg`, `HERO_BG_URL = '/imgs/bedroom_cam_001_res_4096x2048_00.jpg'`. (Файл размещён в `public/`, а не в `src/assets/`, как было в исходной формулировке: 3 МБ JPG нецелесообразно прогонять через бандлер.)
- [x] Перевести строки локалей `uz` и `en` — переведены вручную
- [ ] Уточнить тексты миссии, FAQ-ответов, описаний услуг с клиентом
- [ ] Обновить контактные данные (телефон, соцсети, email, адрес)

## Приоритет 6 — Недостающие секции (по `DESIGN_BRIEF.md`)

В Stitch-экспорт не попали секции, которые были в брифе. Решить с дизайн-агентом / реализовать вручную:

- [ ] Секция **HowWeWork** (4 шага: заявка → выезд и съёмка → обработка и сборка → размещение)
- [ ] Секция **Testimonials** (плейсхолдер)
- [ ] Секция **About** (краткая миссия + цифры-счётчики)

## Приоритет 7 — Маршруты и юридические страницы

- [ ] Создать страницу `/privacy` (Политика конфиденциальности)
- [ ] Создать страницу `/terms` (Условия использования)
- [ ] Подключить маршруты в `App.jsx`

## Приоритет 8 — SEO и аналитика

- [ ] `robots.txt` и `sitemap.xml`
- [ ] OG-image макет 1200×630 в `public/`
- [ ] Микроразметка Schema.org `LocalBusiness`
- [ ] Подключить Yandex.Metrica
- [ ] Подключить Google Analytics 4

## Приоритет 9 — Деплой

- [ ] Настроить GitHub Actions для автосборки и деплоя
- [ ] Определить целевую платформу деплоя (GitHub Pages / Netlify / VPS)
- [x] Проверить production-сборку: `npm run build` — собирается, ~57 КБ gzip JS

---

## Регулярные задачи

- [ ] Поддерживать раздел "Структура репозитория" в `CONTEXT.md` в актуальном состоянии (обновлять после каждого значимого изменения структуры файлов/папок)
- [ ] При любом изменении чекбоксов `[ ]` ↔ `[x]` в этом файле — **в том же коммите** обновлять разделы «Текущее состояние» и «Следующий шаг» в `CONTEXT.md`

## Отложено / под вопросом

- [ ] Нужна ли форма обратной связи — и если да, через что (EmailJS, formspree, свой endpoint)?
- [ ] Аналитика (GA, Yandex.Metrica)?
- [x] Удалить шаблон `musor/` целиком после полной стабилизации новой структуры `src/` — выполнено 2026-04-30 (вопрос про `musor/public/images` снят автоматически)
